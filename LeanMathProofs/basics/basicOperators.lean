namespace LeanMathProofs.basic

def sum {α : Type} [Add α] (a b : α) : α :=
  a + b

def sub {α : Type} [Sub α] (a b : α) : α :=
  a - b

def mult {α : Type} [Mul α] (a b : α) : α :=
  a * b




end LeanMathProofs.basic
